dist: xenial
language: python
cache: pip
notifications:
  email: false
sudo: true

python:
  - "2.7.15"
script:
  - python -m compileall .

python:
  - "3.7"
install:
  - pip install black
script:
  - python -m compileall .
  - black --check -l 99 `git ls-files "*.py"`
